---
const navLinks = [
  { href: '/', label: 'Home' },
  { href: '/articles', label: 'Articles' },
  { href: '/about', label: 'About' },
  { href: '/contact', label: 'Contact' }
];

const currentPath = Astro.url.pathname;
---

<header class="site-header">
  <div class="container header-inner">
    <a href="/" class="brand" aria-label="MockAgent.AI Home">
      <span class="brand-mark">MA</span>
      <span class="brand-name">MockAgent.AI</span>
    </a>

    <nav class="main-nav" aria-label="Main navigation">
      {navLinks.map(link => (
        <a 
          href={link.href} 
          class={currentPath === link.href || currentPath.startsWith(link.href + '/') ? 'active' : ''}
        >
          {link.label}
        </a>
      ))}
    </nav>
  </div>
</header>

<style>
  .site-header {
    background-color: var(--white);
    border-bottom: 1px solid var(--border-light);
    position: sticky;
    top: 0;
    z-index: 100;
  }

  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 64px;
  }

  .brand {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    text-decoration: none;
    color: var(--ink-navy);
  }

  .brand-mark {
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: var(--ink-navy);
    color: var(--white);
    font-family: var(--font-mono);
    font-size: 0.85rem;
    font-weight: 600;
    border-radius: var(--radius-sm);
  }

  .brand-name {
    font-family: var(--font-display);
    font-size: 1.25rem;
    font-weight: 600;
  }

  .main-nav {
    display: flex;
    gap: 1.75rem;
  }

  .main-nav a {
    color: var(--slate-gray);
    font-weight: 500;
    font-size: 0.95rem;
    text-decoration: none;
    padding: 0.25rem 0;
    border-bottom: 2px solid transparent;
    transition: color var(--transition-fast), border-color var(--transition-fast);
  }

  .main-nav a:hover,
  .main-nav a.active {
    color: var(--ink-navy);
    border-bottom-color: var(--brass);
  }

  @media (max-width: 640px) {
    .main-nav {
      gap: 1rem;
    }

    .main-nav a {
      font-size: 0.875rem;
    }

    .brand-name {
      font-size: 1.1rem;
    }
  }
</style>
